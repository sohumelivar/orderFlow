import { Order } from '../../models/index.js';

class DeleteOrderController {
    async deleteOrder (req, res, next) {
        try {
            
        } catch (error) {
            next(error);
        }
    }
}
